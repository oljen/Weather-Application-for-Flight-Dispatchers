- must be launched with PREACT.
- to launch this application, install preact.

INSTRUCTIONS
install preact
run the program wiht 'npm run dev'.

import the src folder into the newly created PREACT folder.
